{
  "name": "yuki-id-portal",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && go build ./cmd/server",
    "server": "go run ./cmd/server",
    "test": "go test ./..."
  }
}
