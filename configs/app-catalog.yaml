# Optional display and access-control overlay for applications returned by
# Logto Management API. Entries here never create standalone portal apps.
#
# Match an application by Logto application id or by name, then override fields
# such as url, icon, sort_order, required_roles, and required_organizations.
#
# Example:
# - id: your-logto-application-id
#   icon: workflow
#   url: https://app.example.com
#   required_roles:
#     - example-user
#   required_organizations: []
#   enabled: true
#   sort_order: 10
[]
